# SOP: AI 协作 — 数据库事务 ctx 业务层改造

## 概述

本文件定义了 AI 子代理协作完成"业务层事务改造"的标准操作流程。核心原则：

- **逐点推进**：每次只处理一个候选修改点，而非批量扫描
- **文档驱动**：子代理产出结构化文档，人工审查后再执行
- **人机协作**：我（主代理）负责审查与决策，子代理负责侦察与实施

## 角色定义

| 角色 | 代理类型 | 负责阶段 | 职责 |
|------|---------|---------|------|
| 子代理（侦察） | **general-purpose** | 阶段一 | 阅读代码 → 分析调用链 → **写候选文档** |
| 我（主代理） | — | 阶段二 | 审查文档，判断优先级，请求用户批准 |
| 子代理（实施） | **general-purpose** | 阶段三 | 根据已批准的文档执行代码修改 |
| 用户 | — | 阶段二 | 最终决策：批准 / 拒绝 / 归档 |

### 代理类型说明

- **阶段一、三必须使用 `general-purpose` 代理**：需要同时具备 Read（读代码）和 Write/Edit（写文档/改代码）能力
- **禁止使用 `Explore` 代理**：Explore 是只读代理，无法写候选文档，也不适合需要精确编辑的阶段三
- `Plan` 代理也不适用：它只做架构规划，不会读具体代码也不会写文件

## 阶段一：侦察 → 产出候选文档

### 输入

主代理告知子代理：

**每次必传**：
- 目标目录范围（如 `api/app/chat/`）
- 参考：`SPEC_SQL_CTX_REFACTOR.md` 中的改造模式

**重复调用时必传（进度上下文）**——子代理不知道此前做过什么，必须告知：
- **已完成列表**：哪些候选点已改造完成（标题 + 涉及文件），避免重复发现
- **已归档列表**：哪些候选点被归档及原因（如"纯读操作，无改造价值"），避免同类误判
- **已侦察文件**：本轮已阅读过的文件列表，提示子代理优先看未侦察的文件
- **经验教训**：此前子代理的常见错误或遗漏（如"不要选纯读函数"）

示例提示片段：
```
## 当前进度（请勿重复发现）
- 已完成：create_session 会话创建原子化 (create_session.py)
- 已归档：无
- 本轮已侦察：create_session.py, send_message.py, cancel_session_task.py
- 提示：优先看 process_pending_messages.py 和 sessions.py
```

### 主代理调用方式

使用 `Agent` 工具，**必须指定 `subagent_type: "general-purpose"`**：

```
Agent(
  subagent_type="general-purpose",
  description="Scout first ctx candidate in <模块名>",
  prompt="..."  # 见下方任务描述模板
)
```

### 子代理工作内容

子代理在目标目录中**找到一个**适合 ctx 改造的业务代码点：

1. 搜索调用了已改造 utils 函数的业务代码
2. 追踪调用链：识别一个业务操作中连续调用的多个 DB 操作
3. 分析当前事务边界：有几个独立 commit？中间失败会怎样？
4. 给出优先级判断和简单方案

### 产出：候选文档

子代理在 `docs/SQL_OP_refactor_plan/` 下创建一个文档，命名格式：
`<模块名>_<功能简述>.md`

文档模板：

```markdown
# 候选：<简短标题>

**状态**: 待审查
**发现日期**: YYYY-MM-DD
**优先级建议**: P0 / P1 / P2

## 涉及文件

- `api/app/xxx/endpoint.py` — 业务入口
- `api/chat/sql_stat/xxx/utils.py` — 调用的 utils 函数

## 当前调用链

```
business_func()
  └→ db_insert_X()        ← 独立事务 #1
  └→ db_update_Y()        ← 独立事务 #2
  └→ db_delete_Z()        ← 独立事务 #3
```

## 当前风险

如果事务 #1 成功但 #2 失败，会导致：xxx 状态不一致

## 改造方案

ctx 创建位置：`business_func()` 入口
ctx 作用域：整个 `business_func()` 调用链
是否需要跨模块透传：是/否，说明路径

## 伪代码

\`\`\`python
# 改造前
async def business_func(data):
    x_id = await db_insert_X(data)   # 独立 commit
    await db_update_Y(x_id, data)    # 独立 commit
    return x_id

# 改造后
async def business_func(data):
    ctx = SQL_OP_ContextData(description="xxx")
    async with ctx:
        x_id = await db_insert_X(data, ctx)
        await db_update_Y(x_id, data, ctx)
        await ctx.commit()
    return x_id
\`\`\`

## 注意事项

- 是否有异常处理需要调整
- 是否有并发考虑
```

## 阶段二：审查 → 决策

### 我（主代理）的工作

1. 阅读候选文档
2. 验证：
   - 调用链分析是否准确
   - 风险判断是否合理
   - 优先级建议是否合适
3. 快速浏览涉及文件的代码，确认子代理没有遗漏

### 决策分类

| 决策 | 条件 | 操作 |
|------|------|------|
| **快速通道** | P0/P1，简单改造（单文件、无副作用、无异常处理调整） | 跳过用户确认，直接进入阶段三执行。完成后积累到模块汇报中 |
| **请求批准** | P0/P1，复杂改造（多文件、异常处理调整、有并发风险） | 更新文档状态为"已批准"，向用户请求执行许可 |
| **归档** | 优先级 P2 或风险可控 | 移动到 `archive/`，在 INDEX 中记录 |
| **退回** | 分析有误或方案不可行 | 标注问题，要求子代理修正或放弃 |

**快速通道 vs 请求批准的判断**：
- 仅需加 `ctx` 创建 + 透传 + commit，无其他逻辑调整 → 快速通道
- 需要重构 try/except、调整控制流、涉及回滚副作用 → 请求批准

### 模块汇报

主代理在完成**一个功能模块的全部候选点**后（或累计处理 3-5 个候选点后），暂停并向用户汇报：

```
模块 <名称> 改造进度：
- 已完成：N 个候选点（列出标题和涉及文件）
- 已归档：M 个候选点（列出标题和归档原因）
是否继续下一个模块？
```

汇报节点定义（按功能模块划分）：
- `api/app/chat/` — chat 模块（文件多，可按子功能分批汇报）
- `api/app/auth/` — 认证模块
- `api/app/system_notification/` — 通知模块
- `api/app/user_file_system/` — 文件系统模块
- `api/app/user_pod_scheduler/` — 调度器模块

## 阶段三：实施 → 修改代码

### 执行者选择

主代理根据改造复杂度决定由谁执行：

| 场景 | 执行者 | 理由 |
|------|--------|------|
| **简单改造**（单文件、机械性改动、方案明确） | **主代理直接执行** | 减少上下文切换，更快；候选文档已足够详细，无需子代理重复阅读 |
| **复杂改造**（多文件、涉及异常处理调整、方案有模糊点需现场判断） | **派遣 general-purpose 子代理** | 隔离风险，主代理保持审查视角；子代理专注实施，产出更完整 |

判断标准：
- 仅需加 `ctx` 创建 + 透传 + commit → 主代理直接做
- 需要重构异常处理、调整控制流、改动超过 ~30 行 → 派遣子代理

### 输入

执行者收到：
- 已批准的候选文档路径
- 用户批准确认

### 工作内容

1. 阅读候选文档，理解方案
2. 按文档中的改造方案修改代码
3. 运行语法检查确认
4. 如有异常处理需要调整，一并处理

### 产出

- 修改后的代码文件
- 验证结果汇报
- 将候选文档状态更新为"已完成"，**移动到 `completed/` 目录**

## 状态流转

```
待审查 ──审查通过──→ 已批准 ──用户确认──→ 执行中 ──完成──→ 已完成
   │                    │
   └── 不适合/低优先级 → 已归档
   └── 分析有误 ──────→ 退回修正
```

## 文件结构

```
项目根目录/
├── SPEC_SQL_CTX_REFACTOR.md       # utils 层改造规范（已完成阶段参考）
├── SOP_SQL_CTX_BUSINESS.md        # 本文件：业务层改造协作 SOP
└── docs/
    └── SQL_OP_refactor_plan/
        ├── INDEX.md               # 所有候选点状态一览
        ├── <module>_<desc>.md     # 待审查 / 已批准 / 执行中的候选文档
        ├── completed/             # 已完成改造的候选文档
        └── archive/               # 已归档（拒绝/低优先级）的候选文档
```
